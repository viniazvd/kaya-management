'use strict';

const test = require('ava');

require('dotenv').config();

const models = require('../../../infra/sequelize/models');

test('create user', async t => {
  const userTest = { name: 'vinitest', email: 'emailtest@gmail.com', password: '123' };
  const newUser = await models.user.build(userTest);
  const result = await newUser.save();

  t.is(result.dataValues.name, 'vinitest');
});

test('update user', async t => {
  const userTest = { name: 'vinitest', email: 'emailtest@gmail.com', password: '123456' };
  const id = 5;
  const result = await models.user.update(userTest, { where: { id } });

  t.is(result.dataValues.password, '123456');
});
//# sourceMappingURL=../../../../node_modules/.cache/ava/e381c824c99fd3dd8c9cf6fdae2f1673.js.map