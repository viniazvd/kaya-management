'use strict';

const test = require('ava');

require('dotenv').config();

const models = require('../../../infra/sequelize/models');

test('create user', async t => {
  const userTest = { name: 'vini', email: 'email@com.br', password: '123456' };
  const newUser = await models.user.build(userTest);
  const result = await newUser.save();
  console.log('resultado: ', result);
  // t.is(result)
});
//# sourceMappingURL=../../../../node_modules/.cache/ava/17773a6284d600e3addeb439fa9ce520.js.map